#receba base e a altura de um triangulo e calcule a sua are
base = (float(input("Qual o tamanho da base?: ")))
altura = (float(input("Qual a altura?: ")))
area = (base * altura) / 2
print("Essa e a Area do triangulo:", area)